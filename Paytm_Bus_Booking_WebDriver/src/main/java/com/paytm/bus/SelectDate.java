package com.paytm.bus;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.firefox.FirefoxDriver;



public class SelectDate {
 /*	public static String trvDate="10-May 2016";
	static String splitter[]=trvDate.split("-");
	String trvDay=splitter[0];
	String month_year=splitter[1];
	public static WebDriver driver=new FirefoxDriver();
	
	public static void selectDate(String trvDay,String month_year){
		List<WebElement> elements=driver.findElements(By.xpath(2 calendars situation));
		for(int i=0;i<elements.size();i++){
			System.out.println(elements.get(i).getText());
			if(elements.get(i).getText().equals(month_year)){
				List<WebElement> days=driver.findElements(By.xpath(xpath of all  days));
				for(WebElement d:days){
					System.out.println(d.getText());
					if(d.getText().equals(trvDay)){
						d.click();
						return;
					}
				}
			}
		}
		click next;
		selectDate(trvDay,month_year);
	}
	public static void main(String[] args){
		System.out.println(splitter[0] +"\n" +splitter[1]);
	}   */

}
